export const queryKeys = {
  currentUser: ["current-user"],
  devDeliveries: ["dev-deliveries"],
  enterpriseAccounts: ["enterprise-accounts"],
  lguAccounts: ["lgu-accounts"],
  systemSettings: ["system-settings"],
} as const;
