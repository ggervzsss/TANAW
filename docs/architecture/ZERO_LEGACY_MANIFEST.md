# TANAW Zero-Legacy Manifest

- Status: **Open — implementation inventory and removal gate**
- Owner: **Program coordinator**
- Independent reviewer: **Unassigned**
- Target contract: **2**

This is the authoritative hard-cutover manifest required by
`TANAW_REPORT_WORKFLOW_IMPLEMENTATION_PLAN.md`. It is intentionally populated
with every superseded object or behavior already known from the review. Agents
MUST add exact tables, columns, indexes, views, routes, DTOs, event types, source
symbols, local stores, jobs, flags, adapters, generated artifacts, and deployment
objects as they discover or create them.

Rows are never deleted to make the list appear complete. They advance to
`verified` with linked evidence and independent review.

## Classification and status

| Classification           | Meaning                                                                                                               |
| ------------------------ | --------------------------------------------------------------------------------------------------------------------- |
| `retain_as_target`       | The object is explicitly part of the target design; prove that only its approved responsibility remains               |
| `transform_then_drop`    | Reconcile required data/behavior into target objects, then physically remove the legacy object                        |
| `drop_without_migration` | The object is invalid, fabricated, duplicated, generated, or has no legitimate state to preserve                      |
| `migration_only_remove`  | Temporary adapter/comparison/checkpoint object allowed only in development or rehearsal and removed before acceptance |

Allowed row status is `open`, `in_progress`, `transformed`, `removed`, or
`verified`. `verified` requires both transformation proof (or an approved reason
none is required) and absence/target proof. An independent reviewer records name,
date, build/migration hash, and evidence location in the row or linked cutover
record.

## Central PostgreSQL objects

| ID     | Legacy object                                                                                                                 | Classification        | Target/disposition                                                                                                            | Owner/wave | Transformation or retention proof                                                                                               | Absence or target-only proof                                                                                        | Status      |
| ------ | ----------------------------------------------------------------------------------------------------------------------------- | --------------------- | ----------------------------------------------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- | ----------- |
| DB-001 | `accounts` table                                                                                                              | `retain_as_target`    | Authentication principal only                                                                                                 | W1-A       | Revisions `20260714_0028` and `20260714_0031` reconcile topology, typed preferences, profile-change workflow, and asset ownership into their target tables | Target catalog/model tests prove enterprise, site, device, gateway, preference JSON, and asset responsibilities are absent from the bounded principal row | transformed |
| DB-002 | Enterprise identity/profile/category/lifecycle columns in `accounts` (exact columns to inventory)                             | `transform_then_drop` | `enterprises` and `enterprise_memberships`                                                                                    | W1-A       | Revision `20260714_0028` fail-closed reconciles one effective enterprise/membership, cuts every runtime reader/writer over, and drops `enterprise_name`, `category`, `manager_name`, `enterprise_id`, `source_kind`, and `mock_run_id` | Rehearsed target catalog contains none of the columns/indexes and account serializers require normalized topology | removed |
| DB-003 | Address, barangay, coordinates/geocode, capacity, and timezone columns in `accounts` (exact columns to inventory)             | `transform_then_drop` | Effective-dated `enterprise_sites`                                                                                            | W1-A       | Revision `20260714_0028` reconciles one stable primary site, invalidates coordinate evidence on address changes, and drops every account location/capacity column | Rehearsed target catalog and runtime searches prove only `enterprise_sites` owns location, provenance, version, and capacity | removed |
| DB-004 | Persistent account gateway/live-status columns and indexes (including `gateway_status`; inventory exact names)                | `transform_then_drop` | `edge_devices` identity plus derived `site_live_state` freshness                                                              | W1-A/W5    | Revision `20260714_0028` reconciles gateway identity, derives presentation state from active devices/live state, and drops `gateway_id`, `gateway_status`, and their legacy index paths | Rehearsed target catalog and runtime searches contain no persistent account gateway/status fallback | removed |
| DB-005 | Account preference JSON used for typed settings or large/base64 profile assets                                                | `transform_then_drop` | Bounded typed preferences plus `account_assets` metadata/object storage                                                       | W1-A/W7-B  | Revision `20260714_0031` fail-closed transforms theme, pending contact workflow, and each validated profile image; seeded rehearsal and `test_assets_postgres.py` verify owner/MIME/size/hash/object replacement | `accounts.preferences_json`, base64 DTOs, routes, readers, and writers are physically absent; authenticated asset reads are owner-scoped | removed |
| DB-006 | `enterprise_telemetry_snapshots` and its indexes                                                                              | `transform_then_drop` | `site_live_state`, `telemetry_observations`, normalized `telemetry_metric_facts`, and `device_health_samples`                  | W1-C/W5    | `20260713_0022` preserves resolved source observations/metric facts/health as explicitly unsequenced and unqualified evidence; `20260714_0033` blocks on every open or unmapped source before removal | Fresh and upgraded PostgreSQL catalogs return zero matching tables; ORM/routes/DTOs/writers are absent and 437 backend tests pass against that target-only catalog | removed |
| DB-007 | `enterprise_report_submissions` and its indexes                                                                               | `transform_then_drop` | Obligations, logical reports, immutable revisions/facts/source batches/review events/intake receipts                          | W1-B/W3/W4 | `20260713_0021` reconciles source rows; `20260714_0033` requires a target revision or explicit resolved/waived exception for every row before removal | Fresh and upgraded catalogs contain no table; OpenAPI/runtime/model searches contain only `/desktop/report-submissions/v2` | removed |
| DB-008 | String/display-label report-period columns and duplicate keys in superseded reporting storage                                | `transform_then_drop` | Natural key `month:Asia/Manila:YYYY-MM` plus central `reporting_periods` UUID                                                 | W1-B       | `20260713_0021` strictly maps full-month labels and records ambiguous/duplicate rows as blocking exceptions; `20260714_0033` refuses open exceptions | Superseded source table/columns are absent and runtime commands require canonical period identity                   | removed |
| DB-009 | Mutable final fact/source-copy columns in `final_reports`                                                                     | `transform_then_drop` | Logical `report_finalizations` parent plus immutable `final_report_versions`                                                  | W1-B/W4-B  | `20260713_0024` reconciles exact scope, totals, actor/time, content hash, and source ownership; `20260714_0033` requires a deterministic target finalization or explicit resolution | Target-only catalog/model/OpenAPI contain `report_finalizations` and no mutable `final_reports` table               | removed |
| DB-010 | `final_report_sources` and non-FK source identifiers                                                                          | `transform_then_drop` | `final_report_items.report_revision_id` exact FK                                                                              | W1-B/W4-B  | `20260713_0024` resolves each imported source to one immutable accepted revision and records ambiguity as blocking; `20260714_0033` removes the source table only after parent reconciliation | Catalog contains no source-copy table; target items retain exact constrained revision ownership                    | removed |
| DB-011 | General activity-log records used as the sole report/final workflow audit                                                     | `transform_then_drop` | Dedicated append-only `report_review_events` and `final_report_events`; general log may remain for non-authoritative activity | W3-D/W7-C  | Target submit/return/accept/reopen/finalize and artifact transitions record server actor/time, versions, reason, and exact immutable ownership; PostgreSQL tests purge convenience logs after acceptance/finalization and retain both event chains | Retention code never age-purges report/final business evidence; official reads/rendering use dedicated immutable events, not `activity_logs` | removed |
| DB-012 | Attachment JSON/base64 content in `support_tickets`                                                                           | `transform_then_drop` | Ticket core plus `support_attachments` metadata/object storage                                                                | W7-B       | Revision `20260714_0031` validates and reconciles each attachment ordinal, MIME, size, SHA-256, owner ticket, and immutable object; seeded rehearsal and PostgreSQL tests verify object existence/access | `support_tickets.attachments_json`, data-URL DTOs, readers, writers, and index-based download route are physically absent; ticket reads return bounded metadata | removed |
| DB-013 | Legacy string UUID primary/foreign-key columns that represent identities (inventory all occurrences)                          | `transform_then_drop` | Native PostgreSQL UUID PK/FK; business identifiers remain separately constrained                                              | W1/W7      | Topology/reporting identifiers are native UUID in `0020`/`0021`; account actor/member FK conversion and full inventory remain   | Catalog proves identity PK/FKs are native UUID and obsolete string columns/indexes absent                           | in_progress |
| DB-014 | Weak string status/severity/source fields and unvalidated relationships in notifications/alerts (inventory exact constraints) | `transform_then_drop` | Constrained target enums/checks, lifecycle/dedup fields, and real FKs where structural                                        | W3-C/W3-D  | Revision `20260714_0030` adds one FK-scoped condition per official site, count/age opening thresholds, recovery hysteresis, collision-safe alert allocation, durable transitions, and automatic resolution; remaining generic alert/notification constraints still require cutover | Catalog proves target constraints/indexes; invalid values and orphan inserts fail                                   | in_progress |
| DB-015 | `email_outbox` and delivery-attempt relationship                                                                              | `retain_as_target`    | Keep one cohesive delivery lifecycle unless measured evidence creates a separate approved ADR                                 | W7 review  | Delivery rows/attempts and retry lifecycle remain intact                                                                        | Review proves reporting work did not create a duplicate email queue or split without cause                          | open        |
| DB-016 | Core `user_notifications`, `operational_alerts`, and `support_tickets` rows                                                   | `retain_as_target`    | Retain bounded core records with target constraints; split only large/repeating children                                      | W3/W7      | Counts, ownership, lifecycle, deduplication, and retained core content reconcile                                                | Catalog/model review proves approved target responsibilities only                                                   | open        |

## Local SQLite and file objects

| ID       | Legacy object                                                                                                                     | Classification           | Target/disposition                                                                                                              | Owner/wave     | Transformation or retention proof                                                                                              | Absence or target-only proof                                                                   | Status      |
| -------- | --------------------------------------------------------------------------------------------------------------------------------- | ------------------------ | ------------------------------------------------------------------------------------------------------------------------------- | -------------- | ------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------- | ----------- |
| EDGE-001 | Legacy `count_events` layout, including global `submitted_report_id`/`synced_at` membership and missing canonical period identity | `transform_then_drop`    | Target event ledger with event UUID, camera, UTC capture, business date, period key, quality, and exact source-batch membership | W1-D/W2        | Schema v5 validates canonical identity and claims, then transactionally rebuilds events with UUID PK, period/camera FKs, sequence uniqueness, payload schema version, and exact child membership | Catalog tests prove `payload_json`, `submitted_report_id`, `synced_at`, and their indexes are physically absent | removed |
| EDGE-002 | `count_snapshots` as raw/history/dashboard source                                                                                 | `transform_then_drop`    | Defined hourly/daily `metric_rollups` plus current live evidence                                                                | W1-D/W2-C      | Schema v5 migrates only the latest typed/redacted state per camera into `camera_live_state`; v4 rollups retain non-identifying history | `count_snapshots`, its index, snapshot writer, purge branch, credential reader, and DTO count are absent | removed |
| EDGE-003 | Mutable `report_submissions` table and sync flags                                                                                 | `transform_then_drop`    | `local_reports`, immutable `local_report_revisions`, source batches, `sync_outbox_items`, and attempts                          | W1-D/W2-B/W3-B | Schema v5 blocks on missing target mappings, reconciles exact claims, then drops the legacy table; local API creates revisions at `POST /reports/local` | Target catalog/runtime/CLI contain no mutable table or `/reports/local-submit` alias | removed |
| EDGE-004 | Global `mark_events_synced()` and `mark_report_synced()` behavior                                                                 | `drop_without_migration` | Exact acknowledgement of one outbox item/revision/source batch                                                                  | W2-B/W3-B      | Exact ready/ack/failure APIs and retry/dead-letter tests replace global mutation                                                | Runtime symbols, endpoints, clients, and callers are absent; only negative absence tests remain | removed     |
| EDGE-005 | `events.jsonl` duplicate event persistence in `session_store.py` and all surviving copies                                         | `transform_then_drop`    | One transactional SQLite ledger; retained rollups only                                                                          | W1-D/W2-C      | Pre-cutover reconciliation imported stable event identities before target acceptance                                           | `events.jsonl`, its path/import method, startup/purge reader, CLI handling, and migration test are physically absent | removed |
| EDGE-006 | Unversioned/ad-hoc SQLite schema mutation and migration-shadow objects                                                            | `migration_only_remove`  | Direct version-6 target catalog plus pre-cutover external checkpoint                                                            | W1-D           | Pre-cutover schema v5 migration validated mappings and external recovery; target release advances to direct schema v6           | Runtime accepts/creates only v6, rejects older stores without mutation, contains no v1-v5 migration modules or `local_schema_migrations`, and passes interruption/catalog tests | removed |
| EDGE-007 | All-open-event `metrics_summary()` selection and display-period fallback                                                          | `drop_without_migration` | Period-key/window-scoped rollup and report queries                                                                              | W2-A/W2-B      | Period-scoped summary/report selection and concurrent June/July tests pass                                                      | Target summary resolver uses one canonical period/window; report request rejects display labels and missing identities | removed |
| EDGE-008 | Raw event, sighting, identity, snapshot, and embedding copies beyond target retention                                             | `transform_then_drop`    | Bounded local identity lifecycle and retained non-identifying rollups                                                           | W2-C           | Unified purge removes eligible events, snapshots, sightings, identities, and orphaned embeddings while retaining rollups        | Final retention schedule and disk/SQLite expiry scan remain                                    | in_progress |
| EDGE-009 | Direct per-event SQLite connections/synchronous multi-store writes without target WAL/serialization                               | `drop_without_migration` | Serialized/batched transactional writer, WAL, busy timeout, durable gap/error signal                                            | W2-C           | WAL, foreign keys, 5-second busy timeout, immediate report transaction, and contention test pass; single writer remains        | Repository search and runtime instrumentation prove one target writer path                     | in_progress |

## Backend API, service, and event objects

| ID      | Legacy object                                                                                                                          | Classification           | Target/disposition                                                                                                      | Owner/wave | Transformation or retention proof                                       | Absence or target-only proof                                                               | Status |
| ------- | -------------------------------------------------------------------------------------------------------------------------------------- | ------------------------ | ----------------------------------------------------------------------------------------------------------------------- | ---------- | ----------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ | ------ |
| API-001 | Report-submission v1 routes, DTOs, payloads, string periods, and mutable update semantics in `backend-tanaw/app/features/operational/` | `transform_then_drop`    | Report command/intake v2                                                                                                | W3-A/W7-A  | Registered v2 intake validates canonical periods/windows, exact source lineage, idempotency/hash, and deterministic acknowledgements | `test_operational_target_contract.py` and repository search prove the v1 routes, schemas, parsers, models, and writers are absent | removed |
| API-002 | Telemetry v1 routes/DTOs/events and append/latest-by-received-time semantics                                                           | `transform_then_drop`    | Sequenced telemetry v2 and replace-if-newer live projection                                                             | W5-A/W7-A  | `20260713_0022` provides epoch/sequence persistence and database guards; registered v2 epoch/observation commands enforce exact replay and monotonic site projection; desktop publishes only durable v2 commands | OpenAPI contains only epoch/observation v2 commands; v1 latest/summary routes, DTOs, events, models, and frontend clients are absent | removed |
| API-003 | Client-controlled enterprise, `preparedBy`, actor, role, or `sourceKind` classification                                                | `drop_without_migration` | Authenticated server-derived context                                                                                    | W3/W4/W5   | Report and telemetry v2 derive enterprise, site, device, camera lineage, classification, actor, and role from authenticated membership/topology; simulation uses separate authenticated routes and classified records | Central v2 schemas and strict local correction/camera schemas reject privileged/extra fields; Electron IPC does not forward them | removed |
| API-004 | Separate post-commit report notification/log/WebSocket side effects                                                                    | `transform_then_drop`    | One business transaction plus durable domain event/outbox                                                               | W3-A/W3-D  | Report commands enqueue atomically; token-fenced `SKIP LOCKED` delivery drives source-revalidated deterministic notification projections and transactional realtime publication | Superseded report commands and their immediate broadcast/log/notification helpers are physically removed           | removed |
| API-005 | In-process-only WebSocket fan-out in `operational/websocket.py`                                                                        | `transform_then_drop`    | Broker/pub-sub adapter fed by transactional domain events                                                               | W3-D/W7-A  | Transaction-commit-scoped PostgreSQL publish plus one reconnecting subscriber per API process passes two-runtime fan-out, deduplication, reconnect, simulation-isolation, and refetch-envelope tests | Remaining direct legacy broadcasts must be removed; the local socket hub remains only as each process's broker subscriber sink | in_progress |
| API-006 | Current `operational/router.py`, `service.py`, `schemas.py`, and mixed `models.py` monolith boundaries                                 | `transform_then_drop`    | `telemetry`, `reporting`, `final_reports`, `alerts`, `notifications`, `support`, `simulation`, and event/outbox modules | W7-A       | Target `reporting` and `final_reports` modules own v2 intake, Staff workflow commands, and authoritative immutable list/detail reads; remaining domains and old-module removal remain | Old modules/imports absent; transaction ownership and pagination review passes             | in_progress |
| API-007 | Unprotected select-max report/final code allocation and permissive transition paths                                                    | `drop_without_migration` | Database-backed collision-safe allocation, locks/constraints, expected versions, allowed-transition table               | W1-B/W4    | Report v2 locks rows and enforces explicit submit/return/accept/reopen actions with exact versions and reasons; final allocation remains | No select-max allocator or free-form status mutation path remains                          | in_progress |
| API-008 | Fixed 500-row list caps and final-report N+1 reads                                                                                     | `drop_without_migration` | Deterministic cursor/page metadata and bounded eager/set-based reads                                                    | W7-A       | Target Staff report/finalization list endpoints use filter-bound keyset cursors with explicit page metadata; list children are bulk-loaded and immutable detail graphs use a fixed number of set-based queries | Query-count/index-plan proof plus repository searches must show only bounded target readers remain | in_progress |
| API-009 | Old summary scans over latest telemetry plus reports on every heartbeat                                                                | `drop_without_migration` | Indexed/materialized live-state and bounded report projections                                                          | W5/W7      | Target map/dashboard readers use indexed `site_live_state` and bounded reporting projections                                  | Latest/summary scan services, routes, DTOs, websocket events, and frontend hooks are absent | removed |
| API-010 | v1 domain-event names/payloads, route aliases, generated schemas, and compatibility registrations                                      | `transform_then_drop`    | Contract-version-2 events and target OpenAPI only                                                                       | W3-D/W8    | Target report, finalization, obligation, epoch, and observation events produce version-only `resource.invalidated` envelopes and idempotent projections | Operational websocket schema/authorization and OpenAPI absence tests admit only current event/path contracts       | removed |

## Portal objects

| ID      | Legacy object                                                                                                           | Classification           | Target/disposition                                                              | Owner/wave | Transformation or retention proof                                                | Absence or target-only proof                                                  | Status |
| ------- | ----------------------------------------------------------------------------------------------------------------------- | ------------------------ | ------------------------------------------------------------------------------- | ---------- | -------------------------------------------------------------------------------- | ----------------------------------------------------------------------------- | ------ |
| WEB-001 | Fabricated camera rows, month-end timestamps, and source proportions in `ReportReviewModal.tsx`                         | `drop_without_migration` | Persisted lineage or explicit missing-data presentation                         | W0-C/W4-C  | Not applicable; missing fixtures stay missing                                    | Presentation tests and source search prove constants/fallback branches absent | removed |
| WEB-002 | Hardcoded audit actors/times in `FinalReportViewer.tsx`                                                                 | `drop_without_migration` | Server-authenticated `final_report_events`                                      | W0-C/W4-C  | Not applicable; missing audit data remains explicit                              | Missing-event tests show no synthesized version, actor, or timestamp          | removed |
| WEB-003 | Fixed demographic/residence ratios in `dotDemographics.ts` and PDF utilities                                            | `drop_without_migration` | Persisted demographic facts with provenance/quality or missing data             | W0-C/W4-C  | Not applicable; absent and partial demographic fixtures remain unknown/incomplete | Ratio generators are absent; UI/PDF tests contain no invented facts           | removed |
| WEB-004 | Filtered `StaffBatchReportsPage.tsx` selection labeled citywide by `DotReportTable.tsx`/PDF/title paths                 | `drop_without_migration` | Persisted `citywide`, `barangay`, or `enterprise_selection` scope               | W4-B/W4-C  | Existing valid final memberships map to explicit scope or block in quarantine    | No client hardcoded citywide label can override server scope                  | open   |
| WEB-005 | Legacy Zustand report fallback in `src/app/store/reportStore.ts`                                                        | `drop_without_migration` | Account-scoped server queries using stable IDs/current detail                   | W4-C       | Portal reports and notifications now use backend queries/records only             | File, exports, imports, persisted fallback, and synthetic-period job are absent | removed |
| WEB-006 | Mutable/duplicate Staff analytics transforms in `StaffAnalyticsPage.tsx` and ineligible Pending/Returned totals         | `drop_without_migration` | Pure target transforms over server-defined accepted/consolidated facts          | W4-C       | Pure-transform fixtures include only latest accepted/consolidated facts           | Mutation path is absent and accepted-status/duplicate-period tests pass        | removed |
| WEB-007 | Global five-minute protected query cache and auth-boundary residue in `queryClient.ts`/`authStore.ts`                   | `drop_without_migration` | Account/role/filter-scoped keys and protected-cache reset                       | W5-C       | Not applicable                                                                   | Cross-account/logout/reconnect tests and scoped-key audit pass                | removed |
| WEB-008 | WebSocket `setQueryData` path that creates a one-item authoritative list from an empty cache in `useOperationalSync.ts` | `drop_without_migration` | Resource-version event followed by safe patch or invalidation/refetch           | W5-C/W5-D  | Not applicable                                                                   | First-event test invalidates/refetches and mutation patches preserve absent caches | removed |
| WEB-009 | Separate map/registry identity/live-state adapters and “Stable” trend fallback                                          | `transform_then_drop`    | Shared site/live-state query with freshness/null semantics                      | W5-C       | Admin Map now pages the canonical `/operational/sites/v2` registry, keys markers by site, includes unlinked sites, refetches every 30 seconds/on version invalidation, and preserves missing/stale metrics as null | Portal source/tests prove the account/map adapter and trend fallback are absent; freshness changes without a new edge event | removed |
| WEB-010 | Modal selection that stores stale report/final row objects                                                              | `drop_without_migration` | Stable selected ID plus authoritative detail query                              | W4-C       | Not applicable                                                                   | Open report/final selections retain only IDs and derive current query records | removed |
| WEB-011 | Multiple activity/notification WebSocket consumers and incomplete final/intake invalidation                             | `transform_then_drop`    | One shared subscription per stream and targeted list/detail/intake invalidation | W5-D       | Operational events share one bridge and invalidate report/final/detail/intake caches | One operational consumer remains; event invalidation and reconnect tests pass | removed |
| WEB-012 | Legacy report/final DTOs, API clients, PDF fallback schemas, and generated v1 artifacts                                 | `transform_then_drop`    | Version-2 generated/shared schemas and immutable artifact reads                 | W4-C/W8    | Valid fields and exports reconcile to target fixtures/hashes                     | Build/package/OpenAPI/type search proves no v1 schema or fallback bundled     | open   |

## Desktop and local ML code objects

| ID       | Legacy object                                                                                                                                                   | Classification           | Target/disposition                                                                                  | Owner/wave | Transformation or retention proof                                         | Absence or target-only proof                                                                                       | Status |
| -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------ | --------------------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------ | ------ |
| DESK-001 | Pre-ack `unsyncedEvents`, newest-first abort queue, global marking, and non-durable retry logic in `cloud-sync.ts`                                              | `transform_then_drop`    | Durable fair outbox with exact acknowledgement, backoff, dead letter, and hash validation           | W3-B/W3-C  | Schema v4 transforms recognized commands; exact command/hash/resource acknowledgement, loss/replay, retry, and dead-letter tests pass | Global/pre-ack/report-ID marking symbols and branches are absent                                                  | removed |
| DESK-002 | `ReportsView.tsx` fallback from missing period to the current period/display string                                                                             | `drop_without_migration` | Required canonical natural key and explicit unclassified error                                      | W2-A/W3-B  | Not applicable                                                            | Boundary/missing-period tests and source search prove fallback absent                                              | open   |
| DESK-003 | Legacy desktop report/telemetry DTOs and API paths                                                                                                              | `transform_then_drop`    | Version-2 envelopes and deterministic acknowledgements                                              | W3/W5/W8   | Report submission sends the exact v2 outbox command and acknowledges only the matching revision; enterprise history follows every cursor; telemetry discovers canonical topology and sends durable sequenced v2 commands with exact acknowledgement validation | Desktop/API/IPC/package searches contain only report/telemetry v2 paths and canonical camera-start fields; old aliases and mutable DTOs are absent | removed |
| DESK-004 | Hardcoded ML port 8765 and trust of any `/health` responder in `electron/main.ts` and camera service code                                                       | `drop_without_migration` | Spawned-child PID/launch identity, ephemeral loopback port, authenticated challenge                 | W6-A       | Not applicable                                                            | Rogue-port, wrong-PID, launch/release/challenge, restart, documentation, and package searches pass                 | removed |
| DESK-005 | Camera credentials, local capabilities, or RTSP URLs exposed to renderer, URL, log, or broad IPC                                                                | `transform_then_drop`    | Main-process secure ownership and narrow typed IPC/custom media protocol                            | W6-B       | Pre-cutover builds transformed browser/plain stores into endpoint-bound OS `safeStorage`; ML snapshots scrub and fail closed on secret residue | Target renderer has no credential/localStorage migration code; main accepts only strict v3 `safeStorage`; network/log/package secret and revocation tests pass | removed |
| ML-001   | Unauthenticated REST, control, data, health, WebSocket, and stream routes in `ml-service/app/main.py`                                                           | `drop_without_migration` | Per-launch capability on every surface                                                              | W6-A       | Not applicable                                                            | Unauthorized REST/WS/stream matrix fails closed; scoped session and expiry tests pass                              | removed |
| ML-002   | Permissive `null`/arbitrary-localhost CORS                                                                                                                      | `drop_without_migration` | No browser CORS; renderer communicates through controlled Electron main IPC/proxy                   | W6-A       | Not applicable                                                            | No CORS middleware/origin bypass remains; renderer uses named IPC and a controlled media protocol                  | removed |
| ML-003   | Capability transport through arguments/environment/URL or any secret logging introduced during migration                                                        | `migration_only_remove`  | Inherited private bootstrap pipe and allowlist logging                                              | W6-A       | Not applicable                                                            | Capability uses stdin/private readiness pipe, rotates per launch, and is absent from args/env/URL/log/renderer scans | removed |
| ML-004   | Camera loop terminal stop after transient stream failure and unrecorded downtime                                                                                | `transform_then_drop`    | Reconnect state machine, bounded jittered backoff, sessions, and coverage gaps                      | W2-D       | Existing sessions use explicit recoverable/nonrecoverable states and durable monitoring/gap records | Fault/reconnect/gap tests prove no old stop-only branch remains                                                    | removed |
| ML-005   | Synchronous duplicated event/session/snapshot writes and per-call connection ownership in `camera_manager.py`, `session_store.py`, and `local_metrics_store.py` | `transform_then_drop`    | One transactional writer/ledger and explicit rollups/coverage                                       | W2-C/W7    | Same-database writes serialize with bounded busy retry; duplicate JSONL/snapshot writes are removed and rollup/purge fault tests pass | Final single-writer module split and runtime instrumentation remain                                                | in_progress |
| ML-006   | Legacy monolithic responsibilities in `local_metrics_store.py` and `camera_manager.py` after target behavior stabilizes                                         | `transform_then_drop`    | Ledger, rollup, outbox, retention, capture, reconnect, and coverage modules with explicit ownership | W2/W7      | Behavior contract suite passes after module move                          | Old modules are removed or contain only an explicitly approved target facade with no legacy logic; imports audited | open   |
| ML-007   | Client-controlled `source_kind`/mock-run classification accepted as official                                                                                    | `drop_without_migration` | Authenticated topology-derived `official`/`simulation` classification                               | W3/W5/W6   | Legitimate simulation histories remain internal to explicit simulation state and are sent only to simulation-classified central routes | Strict request tests reject `source_kind`/actor injection; report/correction IPC omits classification; official telemetry rejects non-real local provenance | removed |

## Temporary migration and deployment objects

Every implementation task MUST replace the wildcard rows below with exact object
rows when it creates one.

| ID      | Legacy object                                                                                                       | Classification          | Target/disposition                                                 | Owner/wave       | Transformation or retention proof                                  | Absence or target-only proof                                             | Status |
| ------- | ------------------------------------------------------------------------------------------------------------------- | ----------------------- | ------------------------------------------------------------------ | ---------------- | ------------------------------------------------------------------ | ------------------------------------------------------------------------ | ------ |
| TMP-001 | Any old/new dual writer                                                                                             | `migration_only_remove` | Target writer only                                                 | Creating task/W8 | Shadow comparison and queue drain pass                             | Exact symbol/job/config absent from source and production build          | open   |
| TMP-002 | Any legacy fallback/dual reader or route alias                                                                      | `migration_only_remove` | Target reader/route only                                           | Creating task/W8 | Read comparison passes                                             | OpenAPI, route registry, repository, and package searches prove absence  | open   |
| TMP-003 | Any shadow/comparison/backfill/checkpoint table, column, view, trigger, or index not in the target ERD              | `migration_only_remove` | External evidence archive or approved target migration ledger only | Creating task/W8 | Backfill completes/reconciles and evidence is exported             | PostgreSQL/SQLite catalogs match signed target inventory                 | open   |
| TMP-004 | Feature flags or environment variables enabling v1, compatibility, fabrication, legacy read/write, or bypass auth          | `migration_only_remove` | No production alternative path                                     | Creating task/W8 | Simulation remains an explicit non-production safety mode with server-enforced classification, not a compatibility path | Settings use case-sensitive canonical aliases; old bootstrap/development/mock/environment names, typed-setting fallbacks, display-name account adoption, and single-package mock fallback are absent | removed |
| TMP-005 | Old generated OpenAPI clients, fixtures, mock handlers, snapshots, docs, support runbooks, or packaged assets       | `transform_then_drop`   | Contract-2 generated and documented artifacts                      | A/B/C/D/W8       | Required legitimate fixture scenarios ported                       | Build/package/repository scan contains target terminology/contracts only | open   |
| TMP-006 | Old scheduled/background jobs for summary scans, alert derivation, retention, notification, or realtime fan-out     | `transform_then_drop`   | Target projection/outbox/retention jobs                            | A/W8             | Pending durable work drained/transformed and job effects reconcile | Scheduler registry/deployment/source search prove old jobs absent        | open   |
| TMP-007 | Electron v1/v2/plain/browser camera credential migration parsers and store-version branches                         | `migration_only_remove` | Endpoint-bound OS `safeStorage` v3 records only                    | W6-B/W8          | Pre-cutover migration transformed valid credentials and scrubbed browser/plain copies | `local-trust-boundary.test.ts`, type/lint, and production build prove old versions, plain encoding, and browser migration code absent | removed |

## Required proof record

For each row moving to `verified`, attach or link:

- exact object/symbol names and final classification;
- migration revision/build/release hash;
- input counts, normalized totals/hashes, exceptions, and reviewer disposition;
- the transformation command and output location;
- the absence query/search command and unedited output;
- targeted and full quality/E2E command results;
- reviewer, UTC verification time, and signature/approval reference.

Minimum database absence evidence includes PostgreSQL catalog queries for tables,
columns, views, indexes, triggers, constraints, and functions, plus SQLite
`sqlite_master`, `table_info`, index, trigger, and filesystem checks. A missing
ORM model is not proof that a table was dropped.

Minimum code absence evidence includes repository `rg`, OpenAPI and route/event
registry diffs, generated-client inspection, production build/package search,
environment/deployment configuration search, and a target-only end-to-end run.
A passing unit test is not proof that a compatibility branch is absent.

## Final sign-off

- [ ] Every wildcard/inventory placeholder has been expanded to exact objects.
- [ ] Every `transform_then_drop` row has reconciled transformation evidence.
- [ ] Every `drop_without_migration` row has an approved reason and absence proof.
- [ ] Every `retain_as_target` row has a responsibility/constraint audit.
- [ ] Every `migration_only_remove` row is physically absent from the release.
- [ ] No row remains `open`, `in_progress`, `transformed`, or merely `removed`.
- [ ] External restore rehearsal and target-only end-to-end run passed.
- [ ] Coordinator and independent reviewer signed the exact release inventory.

TANAW MUST remain closed after migration until every row is `verified`. Backup
tables, commented-out code, dormant routes, disabled flags, unregistered DTOs,
and unreferenced files still count as legacy and fail this gate.
